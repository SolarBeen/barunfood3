# 바른푸드 확장 작업 체크리스트

- [x] 메인 및 서브 페이지 섹션에 `prefers-reduced-motion`을 존중하는 스크롤 페이드인 모션을 적용한다.
- [x] 데스크톱 드롭다운 및 모바일 확장 내비게이션에 1·2차 메뉴와 현재 위치 상태를 구현한다.
- [x] 회사소개, 제조·R&D, 제품·브랜드, 파트너십, 뉴스룸의 독립 서브 페이지와 공통 레이아웃을 구현한다.
- [x] 서브 페이지의 링크·상담 CTA·모바일 레이아웃·키보드 초점을 점검한다.
- [x] 타입 검사·프로덕션 빌드·대표 화면 캡처 검증을 완료한다.
- [x] 이전 확장 작업의 최종 체크포인트를 저장했다.
- [x] 제품 카테고리 데이터와 검색 키워드를 정의하고, 필터·검색·무결과 화면을 구현했다.
- [x] 문의 폼의 필수 항목·연락처·이메일·동의 항목에 대한 인라인 유효성 검사를 구현했다.
- [x] 제출 완료 시 접수번호와 후속 절차를 보여주는 프리미엄 완료 팝업을 구현했다.
- [x] 회사소개 페이지에 공개 연혁 기반 타임라인과 인증 현황 슬라이더를 구현했다.
- [x] 신규 기능의 데스크톱·모바일 레이아웃과 제품 필터·무결과·폼 오류 상태를 검증했다.
- [x] 이전 기능 확장의 최종 체크포인트를 저장했다.
- [x] 제품 카드에서 영양 성분·보관·조리·구매 상담 정보를 확인하는 페이지 내 상세 모달을 구현했다.
- [x] 모달 내부 아코디언으로 제품 스펙·영양 성분을 단계적으로 열람할 수 있게 했다.
- [x] 모든 페이지에서 빠르게 상담 창을 여는 접근성 고려 플로팅 액션 버튼을 구현했다.
- [x] 제품 상세·FAB의 데스크톱·모바일·키보드 동작을 검증했다.
- [x] 이전 기능 확장의 최종 체크포인트를 저장했다.
- [x] 제공된 바른푸드 캐릭터 로고를 헤더 브랜드 로고로 적용했다.
- [x] 동일 자산을 브라우저 파비콘과 앱 아이콘 메타데이터에 적용했다.
- [x] 데스크톱·모바일의 로고 비율과 가시성을 검증했다.
- [x] 이전 로고 적용 버전의 최종 체크포인트를 저장했다.
- [x] 제공된 바른푸드 로고를 활용한 1200×630 OG 브랜드 이미지를 제작했다.
- [x] Open Graph·Twitter 공유 메타 태그와 기본 URL을 문서 메타데이터에 적용했다.
- [x] 스크롤에 따라 배경·그림자·로고 크기가 변화하는 접근성 고려 스티키 헤더를 구현했다.
- [x] 데스크톱·모바일에서 공유 메타데이터·헤더 스크롤 모션을 검증했다.
- [x] 이전 OG 이미지와 스티키 헤더 적용 버전의 최종 체크포인트를 저장했다.
- [x] 회사소개·제품 상세 경로별로 맞춤 제목·설명·공유 이미지를 설정하는 SEO 메타 관리 구조를 구현했다.
- [x] 제공 로고와 배치 마크를 활용한 접근성 고려 초기 로딩 애니메이션을 구현했다.
- [x] 제품 상세 모달 하단에 현재 제품을 제외한 추천 제품 슬라이더와 빠른 상세 진입을 구현했다.
- [x] 페이지별 OG 태그·로딩·추천 슬라이더의 데스크톱·모바일 상호작용을 검증했다.
- [x] 이전 공유·로딩·추천 제품 확장의 최종 체크포인트를 저장했다.
- [x] 01 회사소개·02 제조·R&D·03 제품·브랜드의 번호와 기존 경로를 독립 HTML 진입 페이지에 매핑했다.
- [x] `company.html`·`manufacturing.html`·`products.html`을 생성해 각 기존 서브 페이지로 연결했다.
- [x] 독립 HTML의 제목·설명·데스크톱·모바일 화면을 검증했다.
- [x] 이전 01·02·03 독립 HTML 진입 페이지의 최종 체크포인트를 저장했다.
- [x] 모든 서브 페이지에 현재 위치와 상위 경로를 표시하는 공통 브레드크럼을 구현했다.
- [x] 04 파트너십·05 뉴스룸의 독립 HTML 진입 파일과 페이지별 공유 메타데이터를 추가했다.
- [x] 데스크톱·모바일·독립 HTML에서 모든 주 메뉴 링크가 올바른 서브 경로로 연결되게 했다.
- [x] 파트너십 페이지에 공개 고객사·후기 없이 협업 유형과 프로젝트 범위로 구성한 고객 사례 섹션을 추가했다.
- [x] 브레드크럼·독립 HTML·고객 사례의 링크와 반응형 표시를 검증했다.
- [x] 이전 브레드크럼·독립 HTML·고객 사례 확장의 최종 체크포인트를 저장했다.
- [x] 초기 로딩 컴포넌트의 body 스크롤 잠금 해제 시점을 수정했다.
- [x] 초기 로딩 종료 후 페이지 스크롤과 스티키 헤더 동작을 검증했다.
- [x] 스크롤 잠금 수정 버전의 최종 체크포인트를 저장한다.
- [x] 사용자가 GitHub 업로드를 직접 진행하기로 했으며, 에이전트는 `barunfood3`의 접근 권한 확인을 수행하지 않는다.
- [x] 이 작업에서는 `origin` 연결·커밋·푸시를 수행하지 않고, 현재 버전은 체크포인트로만 제공한다.
- [x] 향후 변경의 GitHub 업로드 운영 방식은 이 작업 범위에서 설정·검증하지 않는다.
- [x] 홈·회사소개·제조·제품·파트너십·뉴스룸의 역할·섹션·CTA·표현을 비교 목록화했다.
- [x] 브랜드 일관성으로 유지할 반복과 통합·축소할 실질 중복을 구분했다.
- [x] 영향도·개선 난이도 기준의 페이지별 정리 우선순위와 권고안을 작성했다.
- [x] 메인·회사소개·제조·제품·파트너십·뉴스룸의 단일 핵심 메시지와 전환 목표를 재정의했다.
- [x] 각 페이지에 고유한 정보 레일·증거 블록·CTA를 배치하는 레이아웃 구조를 설계했다.
- [x] 전역 콘텐츠 운영 원칙과 P0–P2 구현 우선순위를 문서화했다.
- [x] 페이지별 히어로·섹션·증거·CTA의 바로 적용 가능한 카피를 작성했다.
- [x] 전역 버튼·검색·폼·빈 상태에 사용할 마이크로카피를 제안했다.
- [x] 카피의 근거 수준과 사실 확인 필요 항목을 구분해 사용 원칙을 문서화했다.
- [x] 메인 페이지를 Route Desk와 최신 기록 중심의 전환 허브로 재구성했다.
- [x] 회사·제조·제품·파트너십·뉴스룸의 히어로 메시지와 CTA를 역할별로 교체했다.
- [x] 제조 Control Desk, 파트너십 사전 진단, 제품 파일, 뉴스룸 기록 상태의 정보 레일을 적용했다.
- [x] 데스크톱·모바일·주요 전환 흐름을 검증했다.
- [x] 메시지·레이아웃·카피 개선 버전의 최종 체크포인트를 저장한다.
- [x] 웹사이트 정보 구조 정비를 주제로 한 첫 뉴스룸 발행물을 사실 기반으로 작성하고 목록·상세 화면에 등록한다.
- [x] Resend 기반 상담 이메일 전송을 위해 수신 주소 `infobarun@st-group1.com`, 검증된 발신 도메인·주소, 전송용 API 키를 확인한다.
- [x] 선택한 이메일 전송 방식을 폼 제출 흐름에 연결하고 성공·실패 처리를 구현한다.
- [x] 뉴스 발행물·상담 전송의 화면과 오류 처리를 자동·화면 검증한다.
- [x] 뉴스룸 첫 발행물과 Resend 상담 전송 연동의 최신 체크포인트를 저장한다.
- [x] GitHub 연결 승인은 거부된 상태이며, 별도 연결·동기화는 수행하지 않았다.
- [x] GitHub 승인 거부로 인해 에이전트의 `SolarBeen/barunfood3` 커밋·푸시는 실행하지 않았다.
- [x] `SolarBeen/barunfood3`의 현재 업로드는 사용자가 직접 진행하기로 합의했다.
- [x] 메인 푸터 상단에 레퍼런스 기반의 배경 이미지 상담 허브와 네 가지 문의 경로를 구현한다.
- [x] 상담 허브의 상담 모달·전화·오시는 길 링크와 데스크톱·모바일 레이아웃을 검증한다.
- [x] 제공된 제조 영상을 웹 자산으로 업로드하고 메인 히어로 배경 영상으로 적용한다.
- [x] 히어로 영상의 무음 자동 재생·반복·대체 이미지·데스크톱·모바일 카피 대비를 검증한다.
- [x] 개발 서버의 `/newsroom.html` 진입이 SPA 기본 HTML을 받은 뒤 React `NotFound` 화면을 렌더링하는 404를 재현·식별했다.
- [x] 서버 fallback은 HTTP 200으로 SPA 기본 HTML을 제공함을 확인했고, 누락된 독립 HTML React 별칭과 내부 `.html` 링크를 수정했다.
- [x] 홈·회사·제조·제품·파트너십·뉴스룸·뉴스 상세의 직접 진입과 HTML 진입 주소를 회귀 검증했다.
- [x] 현재 회사소개에는 2008~2014 연혁만 반영돼 있으며, 제공 타임라인의 2015~2021 구간은 아직 반영되지 않았음을 확인했다.
- [x] 2015~2021 추가 연혁의 공개 근거를 공식 연혁 페이지와 고해상도 원본 OCR로 교차 확인하고, 2021년 불확실한 세부 제품명은 일반화한다.
- [x] 검증된 추가 연혁을 회사소개 타임라인과 출처 메모에 반영하고 화면을 검증한다.
- [x] 제공된 제조·제품·공정 이미지와 영상을 용도·구도·중복 기준으로 분류한다.
- [x] 메인·제조/R&D·제품·파트너십·뉴스룸의 자산 배치와 미사용 권고안을 작성한다.
- [x] 2차 메뉴 15개 항목이 현재 독립 페이지인지 섹션 앵커인지 경로별로 확인한다.
- [x] 독립 서브페이지로 확장할 항목의 콘텐츠 범위·경로·우선순위를 설계한다.
- [x] 품질·안전과 연구개발의 독립 페이지 콘텐츠·공통 UI 구조를 설계하고 구현한다.
- [x] 요리 담은 피자와 바른 도우의 독립 제품 페이지 콘텐츠·공통 UI 구조를 설계하고 구현한다.
- [x] 4개 독립 페이지의 메뉴, React 경로, 공유 메타데이터, 독립 HTML 진입을 연결한다.
- [x] 4개 독립 페이지의 데스크톱·모바일·직접 URL 진입과 타입·빌드를 검증한다.
- [x] 독립 서브페이지 하단 상담 CTA의 현재 버튼 구조와 공통화 가능한 상호작용 범위를 확인한다.
- [x] 하단 상담 CTA에 호버·포커스·클릭 상태와 모션 감소 대응을 구현하고 검증한다.
- [x] 브라우저에서 하단 상담 CTA의 실제 `:hover` 상태와 버튼·아이콘 변환 값을 기록한다.
- [x] 브라우저에서 하단 상담 CTA의 실제 `:active` 클릭 축소를 기록하고, 모션 감소 적용 경로를 렌더러 수준에서 확인한다.
- [x] 프로덕션 번들 구성과 초기 로드 의존성을 분석해 우선 최적화 대상을 식별한다.
- [x] 페이지 단위 지연 로딩과 공통 라이브러리 청크 분리를 적용한다.
- [x] 초기 경로·서브 페이지의 지연 로딩·번들 크기·타입·빌드를 검증한다.
- [x] 상담 요청 버튼 바로 위에 `tel:` 전화 연결 CTA와 대표 전화번호를 추가한다.
- [x] 데스크톱·모바일에서 전화 CTA의 표시·접근성·연결 주소를 검증한다.
- [x] 회사소개 03 메뉴에 맞춘 독립 오시는 길 페이지와 `/location` 경로를 구현한다.
- [x] `location.html` 독립 HTML 진입과 기존 오시는 길 링크를 새 경로로 연결한다.
- [x] 주소·전화·외부 지도 길찾기·데스크톱·모바일 화면을 검증한다.
- [x] 회사소개·제조·제품·파트너십의 하위 메뉴별 독립 페이지와 HTML 진입 구현 여부를 점검한다.
- [x] 독립 페이지·섹션 앵커·상담 기능을 구분한 점검 결과와 보완 우선순위를 정리한다.

## 검증 메모

제품·브랜드 페이지에서 `도우` 필터를 선택했을 때 `바른 도우`, `푸드서비스 도우` 두 제품 파일만 표시되는 것을 브라우저에서 확인했다.

검색어 `없는제품`을 입력했을 때 무결과 안내와 필터 초기화 버튼이 표시되는 것을 확인했다. 파트너십 페이지의 상단 및 하단 `B2B 상담하기` 버튼도 문의 폼 진입 요소로 표시된다.

빈 문의 폼을 제출했을 때 회사명·연락처·이메일·문의 내용·개인정보 동의에 대한 인라인 오류와 상단 요약 안내가 표시되는 것을 확인했다.

테스트용 회사명·연락처·이메일·문의 내용과 개인정보 동의를 유효한 형식으로 입력해 제출 가능 상태를 준비했다.

브라우저 자동화에서 체크박스 선택 상태가 반영되지 않아 완료 팝업의 브라우저 제출 검증은 별도 보완이 필요하다. 코드 수준의 유효성 검사와 완료 상태 전환은 구현되어 있다.

제품·브랜드 페이지에서 신규 제품 상세 버튼과 전역 상담 FAB의 표시를 확인했다. 자동화 클릭 후 모달 상태가 반영되지 않아 콘솔·구성 점검을 이어서 진행한다.

제품 상세 버튼의 클릭 이벤트를 재확인해 상세 모달이 열리는 것을 확인했다. 첫 제품의 영양 성분이 기본으로 열리고, 상세 스펙·보관·조리 안내 아코디언과 모달 내 상담 진입 버튼이 표시된다.

제품 페이지 우측 하단의 플로팅 상담 버튼을 클릭했을 때 문의 팝업이 열리는 것을 확인했다. 공통 서브 페이지 레이아웃과 메인 페이지 양쪽에 FAB를 연결했다.

제품 파일형 카드, 제품 상세 모달, 회사 기록형 히어로를 데스크톱·모바일 화면에서 최종 확인했다.

상단에서는 투명 헤더와 원래 크기의 로고가 표시되고, 스크롤 후에는 불투명 배경·그림자·축소된 로고가 적용되는 것을 브라우저에서 확인했다.

브라우저에서 Open Graph·Twitter 메타 태그의 제목·설명·1200×630 공유 이미지 경로가 모두 문서에 반영된 것을 확인했다.

제품 상세 공유 주소(`/products?product=detroit`)에서 맞춤 제품 상세 제목이 적용되고, 초기 로딩 화면이 사라진 뒤 영양 성분·스펙·추천 제품 슬라이더가 표시되는 것을 확인했다.

회사소개 경로에서 `회사소개 | 바른푸드의 기준과 기록` 제목이 적용되고, 제공 로고 기반 초기 로딩 화면이 먼저 표시되는 것을 확인했다.

`company.html#approach` 진입 시 주소가 `/company#approach`로 전환되며, 브레드크럼의 HOME · 01 · 회사소개 표기와 서브 페이지 화면이 정상 표시되는 것을 확인했다.

메인 페이지에서 초기 로딩 화면이 사라진 후 전체 콘텐츠가 표시되고, 하단 섹션이 있는 페이지 높이가 복구되는 것을 확인했다.

초기 로딩 종료 후 body·document의 overflow가 기본값이며, 문서 높이 5,518px이 화면 높이 1,100px보다 커 스크롤 가능한 상태임을 확인했다. 실제 한 화면 아래로 스크롤이 이동하는 것도 확인했다.

기능 업그레이드 후 실제 개발 주소에서 메인 Route Desk·첫 뉴스 발행물 링크가 정상 렌더링되는 것을 확인했다. 상단 `B2B 상담` 버튼으로 상담 모달이 열리고, 유형 선택·필수 입력·숨김 스팸 방지 입력·제출 버튼이 화면에 표시된다. 실제 개인정보가 담긴 테스트 전송은 수행하지 않았다.

`/newsroom/site-record-2026`에서 첫 발행물의 고유 제목·설명·발행일·변경 기록·뉴스룸 복귀 경로가 표시되는 것을 확인했다. `pnpm test`는 Resend 키·발신 도메인 검증, 상담 이메일 HTML 이스케이프, 담당자 수신/회신 주소, 발송 실패 처리를 포함한 6개 테스트를 통과했고, `pnpm check` 및 `pnpm build`도 통과했다.

메인 푸터 바로 위에 배경 이미지·브릭 그라디언트·좌측 상담 카피·우측 네 가지 문의 경로·전화/오시는 길 버튼으로 구성된 상담 허브가 데스크톱과 모바일에서 표시되는 것을 확인했다. 맞춤 개발·OEM/ODM 상담 경로를 실행했을 때 문의 모달이 표시되고 `맞춤 제품 개발` 유형이 활성 상태로 선택되는 것을 확인했다.

제공된 제조 영상이 메인 히어로에서 실제 로드됐으며, 브라우저 기준 `autoplay`, `muted`, `loop`, `playsInline`이 모두 활성 상태이고 미디어 준비 상태는 `readyState: 4`임을 확인했다. 데스크톱·모바일 화면에서 제조 작업자 장면과 좌측 히어로 카피의 대비가 유지됐다. 데이터 절약 속성 변경 시뮬레이션은 현재 브라우저의 연결 API 미지원 상태로 대체 이미지 전환을 트리거하지 못해, 모션 감소 상태 기반 검증을 추가로 진행한다.

현재 브라우저에서는 미디어 쿼리 이벤트를 수동으로 발생시켜도 React 구독 상태가 갱신되지 않았고, 강제 재마운트 시도는 페이지 이동으로 평가가 중단됐다. 실제 대체 이미지는 코드 경로와 단위 테스트로 분리해 추가 검증한다.

히어로 영상 대체 조건을 순수 함수로 분리해, 일반 환경에서는 영상을 표시하고 모션 감소·데이터 절약 환경에서는 기존 이미지를 표시하는 세 가지 조건을 자동 테스트했다. 전체 테스트는 4개 파일, 9개 항목을 통과했고 타입 검사도 통과했다.

404는 개발 서버에서 `/newsroom.html`이 SPA 기본 HTML로 제공될 때 React 라우트에 별칭이 없어 `NotFound` 화면으로 렌더링되며 재현됐다. 서버 fallback은 이미 HTTP 200으로 기본 HTML을 제공하므로 서버 코드는 바꾸지 않았고, 공통 메뉴·브레드크럼을 정식 React 경로로 통일했다. 또한 `company.html`·`manufacturing.html`·`products.html`·`partnership.html`·`newsroom.html`의 React 별칭을 추가했다. 수정 후 `/newsroom.html`은 브라우저에서 뉴스룸 제목·목록을 표시했으며, 8개 경로의 화면 캡처와 전체 HTTP 점검에서도 404 화면 없이 실제 페이지가 표시됐다.

공식 공개 연혁 페이지를 근거로 회사소개 타임라인을 2008~2021로 확장했다. 2015~2021의 제품 개발·수출·시설·품질·특허·인증 기록 중 판독이 불명확한 2020년 두 항목은 제외했으며, 2020년에는 오징어 먹물 플랫 도우·크로플 개발만 반영했다. 확장된 타임라인은 데스크톱과 모바일에서 모두 표시되는 것을 확인했고, 전체 테스트 9건·타입 검사·프로덕션 빌드도 통과했다.

2차 메뉴 15개는 현재 독립 URL이 아니라 5개 상위 페이지의 앵커·상담 CTA로 구성돼 있음을 확인했다. 독립 페이지로는 품질·안전, 연구개발, 요리 담은 피자, 바른 도우 네 항목을 P0로 권고하고, 오시는 길·B2B 상담·자료 요청은 전환 기능으로 유지하도록 정보구조 권고안을 작성했다.

P0 독립 페이지인 `/manufacturing/quality`, `/manufacturing/research`, `/products/pizza`, `/products/dough`를 구현했다. 각 페이지에는 상위 페이지로 돌아가는 브레드크럼, 고유한 PageMeta, 실제 촬영 기반 이미지 레일, 제품 파일·상담 CTA를 적용했다. `quality.html`, `research.html`, `pizza.html`, `dough.html`의 독립 HTML 진입도 추가했고, SPA fallback 상황을 고려한 루트 HTML 별칭 라우트를 함께 등록했다. 4개 React 경로와 4개 HTML 주소는 모두 HTTP 200을 반환했고, 데스크톱·모바일 화면 캡처 및 전체 테스트 9건·타입 검사·프로덕션 빌드를 통과했다.

피자 독립 페이지 하단의 `피자 제품 상담` CTA가 마지막 상담 구간에 표시되는 것을 확인했고, 기본 상태의 흰색 버튼·브릭 텍스트·화살표 아이콘이 대비를 유지한다. 브라우저가 사용하는 1,280×1,100px 뷰포트 기준 실제 CTA 경계와 중심을 조회한 뒤 호버했으며, 버튼이 흰색에서 차콜 배경·흰색 텍스트로 전환되는 것을 확인했다. 키보드 포커스 시 2px 골드 테두리와 4px 여백이 실제 적용되는 것도 확인했다. 공통 CTA 스타일은 클릭 시 0.97배 축소되고, `prefers-reduced-motion` 환경에서는 전역 전환 시간이 최소화된다.

호버 상태에서 브라우저 DOM은 `hovered: true`, 버튼 `translateY(-3px)`, 그림자 `0 14px 28px rgba(36, 35, 33, 0.22)`, 화살표 `translate(4px, -4px)`를 반환했다. CSSOM에는 `.consultation-cta:active { transform: scale(0.97); box-shadow: none; }` 규칙과 모션 감소 미디어 쿼리가 모두 존재함을 확인했다. 현재 브라우저는 모션 감소 선호가 비활성 상태여서, 해당 미디어 쿼리의 적용 자체는 코드·CSSOM 수준으로 확인했다.

제조·R&D 페이지는 초기 로더 종료 후 정상 표시되며, 하단 `제조·품질 기준 문의`는 앵커가 아닌 버튼형 상담 CTA로 제공됨을 확인했다. 실제 눌림 상태 검증을 위해 해당 버튼을 화면 중앙으로 이동해 키보드 이벤트를 기록한다.

포커스된 제조 상담 CTA에 Space 키를 입력하자 상담 모달이 실제로 열렸다. 키 입력 시점의 `:active` 스타일 스냅샷을 이어서 확인해 축소 값까지 기록한다.

브라우저 개발자 프로토콜로 제조 상담 CTA의 active 상태를 실제 강제 렌더링했을 때 `matrix(0.97, 0, 0, 0.97, 0, 0)` 변환과 그림자 제거가 적용됨을 확인했다. 같은 렌더러에서 `prefers-reduced-motion: reduce`를 적용하면 CTA의 전환 시간이 `1e-05s`로 최소화되고, 기본 상태의 변환은 `none`으로 유지됐다. 품질·안전·연구개발·요리 담은 피자·바른 도우의 모바일 화면에서도 하단 CTA가 잘리지 않고 단일 열 흐름으로 표시됐다. 최신 전체 테스트 9건·타입 검사·프로덕션 빌드를 통과했다.

페이지 지연 로딩 적용 뒤 `/`와 `/products/dough`를 직접 열어 초기 로더·공통 헤더·각 페이지의 고유 콘텐츠가 정상 표시되는 것을 확인했다. 제품 도우 페이지는 필요한 이미지·제품 파일·상담 CTA를 지연 로드한 뒤 표시했으며, 라우팅 오류나 빈 화면은 발생하지 않았다.

2026-08-26 성능 최적화에서는 모든 페이지를 `React.lazy`·`Suspense`로 분리하고, Vite의 `react-router`·`vendor`·`ui-primitives`·`icons` 수동 청크를 적용했다. 전역 tRPC/React Query/SuperJSON Provider를 제거하고 상담 모달 전용 `InquiryProvider`로 이동했으며, 홈·공통 서브 레이아웃·제조·제품·파트너십의 상담 호출도 `LazyInquiryDialog`로 전환했다.

최신 프로덕션 HTML이 초기 로드하는 JavaScript는 `react-router` 120.77kB gzip, `vendor` 50.64kB, `ui-primitives` 21.54kB, `icons` 4.68kB, `main` 4.00kB으로 총 201.63kB gzip(677,177 bytes raw)이다. 기존 단일 `main` 249.01kB gzip 대비 47.38kB, 19.0% 감소했으며, 상담 청크 `InquiryDialog` 5.07kB gzip과 대화 상자 전용 tRPC/React Query 런타임은 초기 HTML의 script 목록에 포함되지 않는다.

`pnpm test`(4개 파일·9개 테스트), `pnpm check`, `pnpm build`를 모두 통과했다. 브라우저에서 `/`, `/products/dough` 직접 진입을 확인했고, 도우 페이지 초기 리소스에는 `InquiryDialog`·`InquiryProvider`·tRPC/React Query 모듈이 없었다. 상단 B2B 상담 CTA를 누른 뒤에만 해당 모듈들이 요청되고 모달이 열렸으며, 빈 폼 제출 시 기존의 회사명·연락처·이메일·문의 내용·개인정보 동의 인라인 오류가 표시됐다. 실제 이메일 전송은 수행하지 않았다.

우측 하단의 공통 `CUSTOMER DESK` 플로팅 영역에 상담 요청 버튼 바로 위로 `032-567-1062` 전화 CTA를 추가했다. 이 링크는 `tel:0325671062` 주소와 명확한 접근성 레이블을 사용한다. 데스크톱 브라우저에서 해당 링크의 접근성 레이블·번호·`tel:` 주소를 확인했고, 375×812px 모바일 캡처에서는 번호 CTA가 상담 요청 버튼 바로 위에 선명하게 표시되며 다른 요소와 겹치지 않았다. `FloatingInquiryButton` 정적 마크업 테스트를 포함해 전체 10개 테스트·타입 검사·프로덕션 빌드를 통과했다.

오시는 길은 `/location` React 경로와 `location.html` 독립 진입으로 구현했다. 회사소개 03 메뉴, 푸터 위치 정보, 푸터 상담 허브의 방문·위치 링크를 새 경로로 통일했고, 주소·전화·메일·외부 지도 길찾기·방문 전 안내를 한 화면에 구성했다. 외부 지도 iframe은 환경에 따라 빈 영역이 될 수 있어, 주소와 길찾기 행동이 항상 보이는 클릭형 지도 안내 패널로 대체했다. 최종 375×812px 모바일 캡처에서 패널의 위치 표식·주소·길찾기 문구가 한 카드 안에 표시되고 다음 섹션·플로팅 상담 버튼과 겹치지 않는 것을 확인했다. 데스크톱·`/location.html` 직접 진입도 정상이며, 전체 11개 테스트·타입 검사·프로덕션 빌드를 통과했다.

하위 메뉴 점검 결과, 독립 React 페이지와 독립 HTML 진입은 회사소개(브랜드 스토리), 오시는 길, 제조 역량, 품질·안전, 연구개발, 제품 카탈로그, 요리 담은 피자, 바른 도우, 파트너십 소개에 제공된다. `우리가 일하는 방식`은 `/company#approach`의 섹션 앵커이고, B2B 상담은 `/partnership#inquiry`의 상담 체크리스트·모달 진입 기능이다. `상담 절차`는 현재 `/partnership#process`로 연결되지만 실제 `id="process"` 대상이 없어 독립 페이지가 아니며, 앵커 연결도 보완 대상이다.
